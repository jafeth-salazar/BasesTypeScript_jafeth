(() => {

    const fullname = (firstName:string,lastName:string): string => {
        
        return `${firstName} ${lastName}`
    }
 const name:string = fullname('Salazar','Aaron')
console.log({name})

})()