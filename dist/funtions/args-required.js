"use strict";
(() => {
    const fullname = (firstName, lastName) => {
        return `${firstName} ${lastName}`;
    };
    const name = fullname('Salazar', 'Aaron');
    console.log({ name });
})();
//# sourceMappingURL=args-required.js.map